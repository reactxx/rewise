import 'io.dart' as utils;

class fileSystem {
  //static final _codeDartUtils = r'\rewise\dlibs\utils';

  //static final rwdomInclude = utils.Dir(_codeDartLow + r'\include');
  static final lowRoot = utils.Dir(r'\rewise\rw_low');
  
  //static final rwdomMessages = utils.Dir(_codeDartUtils + r'\lib\src\messages');
  static final utilsRoot = utils.Dir(r'\rewise\dlibs\utils');
}
