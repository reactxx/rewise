//import 'dart:io' as io;
import 'package:test/test.dart' as test;
//import "package:github/server.dart" as g;

main() {
  test.test('fake-test', () async {
    // var dataPath = r'c:\temp2\temp';
    // var link = io.Link(dataPath);
    // if (link.existsSync()) link.delete();
    // link.createSync(r'c:\temp', recursive: true);
  });
}
